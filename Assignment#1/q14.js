var friends = ['Faiq', 'ALi', 'Hammad'];
for (var index = 0; index < friends.length; index++) {
    var element = friends[index];
    console.log('I would like to invite you' + element + 'for dinner.');
}
